# AI 财务审计分析平台开发规范

本文件是本仓库的 Codex 项目级执行规范。所有开发、测试、评审和文档工作都必须遵守本文件，并以可复核、可恢复、最小外发和人工最终决策为基本原则。

## 1 项目目标与当前阶段

本项目面向单人内部审计人员，目标是在 Windows 10 或 Windows 11 的 16GB 内存轻薄本上提供本地优先的 AI 财务审计工作台。

系统采用“本地确定性计算 + 可配置外部模型 API”的方式：

- 原始文件、项目数据、分析结果和审计轨迹默认保存在本机。
- 金额计算、勾稽、趋势、统计指标和风险规则必须由本地代码执行并可复算。
- 外部模型只负责 OCR 或视觉理解、非结构化证据归纳、风险解释和文字草稿。
- AI 只能生成线索、解释和草稿，不得替代审计人员作出最终判断。
- 不引入本地大模型、GPU 推理、Docker、WSL 或服务器级知识库作为 MVP 运行依赖。

仓库当前处于产品设计和 MVP 开发准备阶段，尚无生产级前后端工程。现有页面、金额、企业名称、风险、模型状态和操作成功提示均为原型演示数据，不代表功能已经实现。

## 2 指令与事实来源的优先级

发生冲突时，按以下顺序处理：

1. 用户在当前任务中的明确要求。
2. 本文件中的开发和安全约束。
3. `docs/AI财务审计分析平台_MVP_详细PRD.md` 中的产品范围、需求编号、数据要求和验收标准。
4. `docs/01_AI财务审计分析平台_MVP产品需求说明书.docx`、`docs/02_AI财务审计分析平台_MVP技术架构设计说明书.docx`、`docs/03_AI财务审计分析平台_开源项目选型与许可证评估.docx`、`docs/04_AI财务审计分析平台_MVP开发计划与验收标准.docx` 中不与详细 PRD 冲突的补充内容。
5. `prototypes/v3.2/index.html`、同目录 `README.md`、`DESIGN_AUDIT.md` 和预览图中的视觉、布局、交互与响应式基准。
6. V1、V2、V3 原型，仅用于理解演进历史，不作为新实现基准。

详细 PRD 决定“做什么、数据如何处理、怎样验收”；V3.2 决定“界面如何组织、如何呈现、如何交互”。不得用原型中的模拟行为推翻产品、安全或验收要求。

如果同一优先级的资料互相矛盾，或者高优先级资料没有给出会影响产品范围、架构、安全或审计口径的答案，必须进入 Grill Me 流程，不得自行选择。

## 3 Grill Me 决策机制

### 3.1 必须询问的情况

以下不确定性必须暂停相关实现并向用户提问：

- 新增、删除或改变 P0/P1 产品范围。
- 改变前后端边界、存储方案、任务状态机、模型网关或部署方式。
- 改变金额口径、财务规则、风险分级、证据充分性或审计结论字段。
- 涉及真实数据外发、密钥存储、权限、加密、遥测、远端文件生命周期或严格离线语义。
- 涉及数据库不可逆迁移、批量删除、覆盖历史版本或破坏性 Git 操作。
- 选择尚未批准的模型服务商、OCR 策略、报告模板或许可证风险较高的依赖。
- 需求与 V3.2 基准存在无法同时满足的实质冲突。

### 3.2 提问方式

- 每轮集中提出 3 至 5 个存在依赖关系的问题，避免零散追问。
- 每个问题说明发现的事实、影响范围和推荐答案。
- 优先询问会约束后续决策的上游问题。
- 能通过仓库、文档、代码或测试查明的事实必须先自行核实，不得转问用户。
- 用户确认前，可以继续做无副作用的调查，但不得实施依赖该决策的改动。
- 达成一致后，把稳定决策落实到代码、测试、ADR 或相关文档中，避免重复询问。

### 3.3 无需询问的情况

命名、局部重构、可逆实现细节、测试组织、错误修复和不改变外部行为的技术选择，由 Codex 依据本规范自行决定。不要用提问替代工程判断。

## 4 MVP 范围边界

### 4.1 P0 主链路

开发优先形成以下完整闭环：

1. 创建隔离项目并设置年度、主体、本地路径和模型方案。
2. 批量或增量导入 PDF，执行 SHA-256 去重、损坏文件隔离和可恢复任务处理。
3. 本地解析原生 PDF；扫描页按需逐页进入视觉模型流程。
4. 保存页码、文本块、解析方式、版本及可用坐标，建立 FTS5、向量和 DuckDB 联合取证能力。
5. 本地执行确定性规则、趋势、MAD、结构占比等分析。
6. 使用最小必要证据调用模型生成风险解释，创建带支持证据、反证和不确定性的风险卡片。
7. 由用户复核风险、修改状态并保留完整版本历史。
8. 从已确认风险生成资料清单、访谈问题和 Word、Excel、PDF 工作成果。
9. 全过程记录实际模型、证据范围、规则版本、人工修改和导出快照。

### 4.2 文件类型边界

- PDF 是当前 MVP 的正式 P0 输入闭环。
- V3.2 中出现的 XLSX、CSV、图片导入是交互意图和未来扩展占位，不得据此宣称已进入 P0。
- 在用户确认 Excel 或其他文件类型前，可以设计可扩展接口、Schema、Mock 和禁用态 UI，但不得擅自实现或改变发布范围。

### 4.3 明确不做

- 本地大模型、本地 Embedding 模型和 GPU 推理。
- 多用户、部门、角色、权限和审批体系。
- 全自动三张表深度勾稽、自动责任界定和隐藏思维链展示。
- 领导任期自动拆分、复杂关系图谱、实时流数据和复杂管理驾驶舱。
- 一次性把全部多年资料发送给模型。
- 对全部扫描件承诺字符级坐标高亮。
- 原生移动端应用。响应式 Web 界面不属于此限制。

## 5 V3.2 前端基准

### 5.1 基准保护

- `prototypes/v3.2/` 是只读设计基准，不直接改造成生产应用。
- 未经用户明确同意，不修改、删除或覆盖 V3.2 HTML、说明、设计审计和预览图。
- 生产前端应新建 React + TypeScript 工程，以组件和真实状态重建界面。
- 不复制原型内联脚本作为生产业务逻辑，不把硬编码示例数据搬入生产数据层。

### 5.2 必须保留的信息架构

保留六个一级区域：项目、资料、分析、风险、输出、设置。

项目工作区保留三段式审计流程：

- 左侧线索队列。
- 中部证据分析画布。
- 右侧复核与处置面板。

右侧面板保留判断依据、AI 解释、人工处置三个步骤；桌面端保留专注查看；移动宽度下保留线索、证据、处置的工作区切换。AI 助手必须能明确显示当前上下文范围，并区分“仅使用已选证据”和“项目检索范围”。

### 5.3 视觉与交互原则

- 延续企业分析产品、Swiss 网格和克制的深色金融数据工作台方向。
- 默认深色并支持浅色主题；不得以高饱和装饰色制造视觉噪声。
- 使用分区线、网格和留白建立层级，避免重复嵌套圆角卡片。
- 风险色、警告色和状态色必须具有文字或图标辅助，不得只靠颜色传达状态。
- 动效仅用于 80 至 180ms 的状态反馈和布局切换；禁止自动播放、滚动劫持、循环动画和装饰性入场。
- 完整支持 `prefers-reduced-motion`。
- 保留清晰的键盘焦点、语义化标签、对话框焦点管理和可读的辅助文字。
- 所有异步操作必须显示真实的 pending、success、empty、partial 和 error 状态，禁止伪造成功提示。

### 5.4 响应式和视觉验收

- 覆盖 V3.2 已体现的桌面、笔记本、平板和窄屏布局，并重点检查约 1240、1180、940、620 像素附近的布局变化。
- 使用同一组稳定夹具分别验证深色、浅色、线索/证据/处置切换、处置标签和专注查看。
- 视觉验收应对照 `preview-desktop.png`、`preview-laptop.png`、`preview-mobile.png`、`preview-mobile-decision.png` 和 `preview-light.png`。
- 追求信息层级、关键尺寸、交互状态和响应式行为的一致，不要求把单文件原型的偶然实现逐像素复制。
- 任何影响基准布局的改动都要提供改动前后截图或视觉回归结果。

## 6 目标技术架构

### 6.1 推荐工程结构

首次初始化生产工程前，先提交目录方案和启动方式供用户确认。默认建议：

```text
frontend/          React TypeScript 前端
backend/           FastAPI 应用和业务模块
tests/             跨层测试、验收夹具和端到端测试
data/              本地运行数据，仅保留目录说明和安全示例
templates/         经确认的 Word Excel PDF 模板
scripts/           开发、迁移、打包和验收脚本
docs/              产品、架构、ADR、接口和测试报告
prototypes/v3.2/   只读视觉与交互基准
```

如果现有工程形成了另一套清晰结构，应尊重现状并做最小调整，不为满足示例目录而机械搬迁。

### 6.2 组件边界

- 前端：React + TypeScript，负责界面、表单、状态呈现、PDF.js 阅读器和可访问交互。
- 应用层：FastAPI + Uvicorn，只监听 `127.0.0.1`，提供本地 REST/SSE、输入校验和文件下载。
- 元数据：SQLite，保存项目、文档、任务、风险、笔记、模型档案、缓存、日志和导出记录。
- 分析：DuckDB，负责多年财务数据、规则结果和异常统计。
- 检索：SQLite FTS5 + sqlite-vec；sqlite-vec 在 Windows 验证失败时回退为 FTS5 + 内存余弦检索。
- 任务：SQLite 持久化单工作器，本地 CPU 任务并发固定为 1。
- 模型：LiteLLM SDK + 必要的厂商适配器，不部署 LiteLLM Proxy。
- 文档：PDF.js、pypdf、pdfplumber；报告使用 python-docx、docxtpl、openpyxl 等经许可审查的轻量库。
- 资源：psutil + Windows Job Object 或等效机制，执行进程限制、整机阈值和暂停恢复。

前端不得直接访问外部模型、OCR 或 Embedding 服务。所有真实外发必须经过后端统一模型网关和外发审计器。

### 6.3 版本与依赖

- 不在本文件中锁定未经工程验证的具体依赖版本；版本应记录在锁文件和第三方清单中。
- 新增或替换生产依赖前，说明用途、替代方案、体积、Windows 兼容性、维护状态和许可证影响。
- 优先 MIT、BSD、Apache-2.0 依赖，并保留版权和许可证文本。
- LGPL 依赖保持独立动态依赖边界并在发布前复核。
- GPL、AGPL、修改版 Apache、附加条款或 enterprise 目录代码不得作为闭源 MVP 核心依赖，除非用户和法务明确批准。
- 不因方便引入 Redis、Celery、独立向量数据库、对象存储或多服务编排。

## 7 数据和领域规则

### 7.1 可追溯性

每条风险至少关联：风险编号、状态、等级、触发规则及版本、输入值、基线值、计算结果、模型解释、支持证据、反证、人工意见、实际模型、调用时间和风险版本。

每条证据至少可回溯到项目、文档、页码、块序号、原文、解析方式和解析版本。原生 PDF 尽量保留可用坐标；扫描 PDF 的 P0 保证是准确页码，不得伪造坐标精度。

人工校正、风险状态变化、增量重算和导出不得覆盖历史记录。历史导出不得被新结果静默覆盖。

### 7.2 确定性计算

- 模型不得承担金额求和、勾稽、统计指标或风险规则的最终计算。
- 财务计算使用明确的数据类型和舍入规则，禁止隐式浮点近似改变金额。
- 每条规则具有稳定唯一编号和版本，并对输入、输出、缺失值、口径和边界情况做测试。
- Z-score 只能作为辅助指标，不能单独决定高风险。
- 样本不足、年度缺失、分母变化或会计口径不一致时，结果必须携带不确定性。
- 新证据只重算受影响的年度、主体、科目和风险，旧结论保留为版本。

首批财务规则、风险分级口径和报告模板尚未确认。可以建立扩展点、Schema 和测试夹具，但进入真实业务实现前必须 Grill Me，禁止自行发明审计规则或合规结论。

### 7.3 任务状态机

任务状态使用 `queued`、`running`、`pausing`、`paused`、`retry_wait`、`failed`、`completed`、`cancelled`，并只允许详细 PRD 定义的动作。

- 每个长任务拆成可重入步骤，并在安全点提交状态。
- 重启后已完成步骤不得重复执行、重复生成风险或重复计费。
- 超时、429、余额不足、无效 JSON、服务不可用、磁盘不足和资源暂停必须映射为清晰、可操作的错误。
- 取消任务要保留审计日志并安全清理临时文件。

## 8 模型网关与结构化输出

### 8.1 Provider 抽象

模型档案必须显式记录文本、视觉、JSON Schema、工具调用、Embedding、文件上传、上下文长度和输出上限等能力。业务代码依赖任务能力，不得散落硬编码具体厂商或模型名称。

- 记录实际调用模型，不只记录逻辑模型名称。
- 自动切换备用模型时，在结果和日志中明确标注。
- 向量索引记录模型、维度和版本，不同维度不得混用。
- 供应商原生视觉、文件或 Responses 类能力通过隔离适配器实现。

### 8.2 输出校验

- 视觉识别和风险解释使用版本化 JSON Schema。
- 输出先做严格 Schema 校验，再进入业务层。
- 无效 JSON 最多进行一次可审计的本地修复或低成本模型修复；失败后进入备用模型或人工处理。
- 模型输出不得直接覆盖人工确认内容。
- 不保存或展示隐藏思维链，只保存可展示的依据、证据、计算步骤、规则、假设和不确定性。

## 9 安全 隐私与外发控制

### 9.1 默认安全状态

- 开发和自动化测试默认使用 Mock 或 Fake Provider，不得调用真实模型、OCR、Embedding、Rerank 或远端文件接口。
- 只有用户已明确配置服务商、项目允许外发、严格离线模式关闭且请求通过外发审计器时，才允许真实调用。
- 测试真实外部接口时使用脱敏或合成数据，不提交真实账套、银行流水、凭证、合同、审计底稿或客户资料。

### 9.2 密钥和本地访问

- API Key 使用 Windows DPAPI、系统凭据库或等效安全存储；数据库只保存密钥引用。
- 日志、SQLite、崩溃报告、测试快照和导出物中不得出现明文密钥。
- 密钥输入框默认遮蔽，不提供完整值回显。
- 本地服务只绑定 `127.0.0.1`，浏览器会话使用随机本地令牌，默认禁止局域网访问。

### 9.3 外发审计

每次外部请求至少记录项目、任务、服务商、实际模型、开始和完成时间、耗时、证据编号、文档页码、数据摘要、Token、费用估算、缓存、重试、结果状态和错误。默认不保存完整敏感正文。

- 只发送当前任务所需的最小结构化指标和证据片段。
- 远端文件应设置最短可用生命周期，任务完成后主动删除。
- 删除失败进入持久化重试队列并向用户显示。
- 严格离线模式由统一拦截器拒绝所有外部请求，被阻止的任务必须给出明确原因。
- 离线模式下，本地阅读、FTS、DuckDB、确定性规则和已有结果继续可用。

### 9.4 数据和仓库卫生

- 不提交真实项目数据、API Key、凭据文件、本地数据库、模型响应缓存、导出物或临时页图。
- `data/` 只允许提交 README、空目录占位、Schema、迁移和明确标注的合成夹具。
- 示例数据必须显式标为 synthetic 或 demo，且不得与真实主体混淆。
- 删除项目、原始文件、索引、数据库或导出历史前必须二次确认，并优先使用可恢复策略。

## 10 性能和资源预算

默认目标设备为 4 核 8 线程以上、16GB 内存和 SSD 的 Windows 轻薄本。

- 空闲 CPU 均值低于 5%。
- 应用持续 CPU 不高于 35%，短时峰值不高于 45%。
- 整机 CPU 超过 50% 持续 10 秒时暂停后台任务，低于 30% 持续 10 秒后恢复。
- 后端、索引和任务服务空闲内存合计不高于 2.5GB；常规任务目标不高于 5GB。
- DuckDB 默认内存上限 2GB，线程为逻辑核心 25% 且最多 2 个。
- 本地 CPU 任务并发为 1；外部 API 默认并发为 2，可配置为 1 至 4。
- 系统内存使用超过 70% 时不启动新任务；磁盘剩余低于 10GB 时停止新导入。
- 扫描 PDF 按页渲染，上传或处理完成后及时释放图像内存和临时文件。

不得用降低证据完整性、跳过日志或绕开任务持久化的方式满足性能指标。

## 11 开发工作流

### 11.1 开始任务前

1. 检查 Git 状态并保留用户已有改动，不覆盖无关文件。
2. 阅读与任务直接相关的 PRD 编号、架构章节、验收编号和 V3.2 基准区域。
3. 明确本次改动的范围、不做事项、数据影响、安全影响和验证方式。
4. 如果生产工程尚未初始化，先提交目录、包管理、启动方式和测试策略供用户确认。
5. 如果存在本文件定义的决策缺口，按 Grill Me 流程提问。

### 11.2 实现要求

- 以可运行的纵向切片推进，优先完成项目、PDF、证据、风险和导出的真实闭环。
- 前后端共享的 API 契约和 Schema 必须版本化；前端不得依赖未声明的响应字段。
- 所有输入在边界处校验，领域层不得信任浏览器、文件内容或模型输出。
- 生产路径不得使用静默 fallback、伪成功、空异常处理或无期限 TODO。
- 错误信息同时提供机器可判定错误码和用户可执行的下一步。
- 日志使用结构化字段，不记录密钥和不必要的敏感正文。
- 迁移必须可审查；破坏性迁移需要备份、回滚方案和用户确认。
- 公共行为、数据结构、配置或部署方式改变时同步更新文档。

### 11.3 命令和工具

- 先检查实际的包清单、锁文件、脚本和 README，再运行命令。
- 仓库尚未提供的 `lint`、`test`、`build`、`dev` 或迁移命令不得虚构为已存在或已通过。
- 工程初始化后，把唯一可信的安装、启动、检查、测试、迁移和打包命令写入根 README，并由 CI 使用同一入口。
- 保持依赖锁文件一致，不手工修改生成文件或供应商代码。

## 12 测试与验收门禁

### 12.1 基本原则

- 每项功能改动关联至少一个 PRD 编号、风险控制项或 F01 至 F12 验收项。
- 缺陷修复先建立可失败的回归测试，再修复。
- 单元测试覆盖领域规则、状态机、金额计算、Schema 校验和纯函数。
- 集成测试覆盖 SQLite、DuckDB、文件处理、任务恢复、模型网关和外发拦截器。
- 端到端测试覆盖上传、暂停恢复、证据跳转、风险状态同步、导出和离线阻断。
- 前端测试覆盖键盘操作、焦点、ARIA、窄屏工作区切换、明暗主题和异步错误状态。
- 测试默认离线且可重复，不依赖实时第三方服务、当前日期或随机网络结果。

### 12.2 专项测试

以下变更必须增加专项验证：

- 金额、趋势、MAD、Z-score、结构占比或风险规则：独立复算、精度、缺失值和边界测试。
- 任务队列：幂等、崩溃恢复、重复执行、取消、429、超时、余额不足和无效 JSON。
- 证据链：文档、页码、原文片段、支持或反证方向及解析版本完整性。
- 密钥、外发和离线：日志泄漏检查、回环绑定测试和网络层阻断测试。
- 数据迁移：升级、失败回滚、备份恢复和旧数据兼容。
- 报告导出：文件可打开、字段完整、引用存在、风险状态一致且历史文件不被覆盖。
- 资源治理：在基准设备或等效受控环境执行 P01 至 P07，并记录设备和测量方法。

### 12.3 视觉测试

- 对照 V3.2 预览图验证桌面、笔记本、移动端和浅色主题。
- 检查无意的溢出、遮挡、文本截断、焦点丢失、不可读对比度和布局跳动。
- 测试正常、空、加载、部分成功、错误、离线、资源暂停和长文本状态。
- 使用确定性数据夹具，避免截图因时间、随机数或真实 API 响应持续变化。

### 12.4 完成定义

只有同时满足以下条件，才可以声称任务完成：

- 实现满足对应 PRD、架构和 V3.2 基准，没有扩大范围。
- 相关自动化测试已通过，并报告实际运行的命令和结果。
- 涉及 UI 时已完成相应视口和主题的视觉检查。
- 涉及安全、外发、金额、恢复或迁移时已完成专项测试。
- 没有新引入明文密钥、真实敏感数据、静默失败或未审查依赖。
- 文档、Schema、迁移、第三方清单和测试夹具已按需更新。
- 未验证的项目明确列出，不以“应该可用”替代证据。

测试失败、环境缺失或用户决策未完成时，必须准确报告为未完成或受阻，不得降低门禁或删除测试来获得通过。

## 13 代码评审重点

评审优先寻找会造成错误审计结论、证据丢失、敏感数据外发、不可恢复任务或资源失控的问题，而不是只检查格式。

- 检查模型是否越权进行金额计算或覆盖人工结论。
- 检查风险是否缺少来源页码、反证、不确定性、规则版本或历史版本。
- 检查真实外发是否绕过统一网关、同意状态、最小上下文或审计日志。
- 检查离线模式是否存在旁路，包括 Embedding、Rerank、遥测和远端文件接口。
- 检查任务步骤是否幂等、可暂停、可恢复且不会重复计费。
- 检查金额精度、时间范围、缺失年度、分母和会计口径。
- 检查前端是否把模拟状态、硬编码数据或 toast 当成后端成功。
- 检查 SQLite 长事务、并发写和 DuckDB 无上限查询。
- 检查依赖许可证、Windows 打包、体积和常驻服务影响。
- 检查无障碍、响应式和 `prefers-reduced-motion` 是否退化。

发现问题时说明影响、复现条件、证据和安全修复路径；不要用没有依据的偏好阻塞改动。

## 14 待产品决策登记

以下事项在进入对应实现前必须由用户确认，并记录到 ADR 或产品文档：

- 首批固定报告模板及模板负责人。
- 首批确定性财务规则和风险分级口径。
- 允许使用的外部模型服务商及数据处理政策。
- 是否需要项目级密码或本地数据库整体加密。
- Excel、CSV 和图片是否进入正式 MVP 输入范围。
- OCR 是否允许上传整页图像，是否要求本地预脱敏。
- Word、Excel、PDF 导出的优先级和业务验收人。

在决策完成前，保持边界清晰：可以建设接口、Schema、Mock、禁用态界面和合成测试数据，不得把假设包装成已批准需求。
